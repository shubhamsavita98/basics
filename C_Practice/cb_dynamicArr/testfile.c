#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    char *ptr;
    ptr = "This is character pointer.";
    printf("%s", ptr); //prints the character
    printf("%d",strlen(ptr));
    return 0;

}
